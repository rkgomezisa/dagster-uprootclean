type: dagster_dlt.DltLoadCollectionComponent

attributes:
  loads:
    - source: .loads.source
      pipeline: .loads.pipeline
      translation:
        group_name: shiphero
        description: "Loads orders data with line items"